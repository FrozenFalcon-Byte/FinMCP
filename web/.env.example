# Origin of the FinMCP API. Leave empty for local dev (Vite proxies /api and /mcp to :8000) and for the
# single-process build where the API serves this app. Set it on Vercel to the backend's public URL:
# VITE_API_BASE=https://<hf-user>-finmcp-api.hf.space
VITE_API_BASE=
